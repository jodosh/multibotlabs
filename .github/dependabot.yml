version: 2
updates:
  # Keep workflow actions current so they don't drift onto deprecated
  # runtimes. npm is deliberately not covered here.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
