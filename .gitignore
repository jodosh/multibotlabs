node_modules/
out/
dist/
*.log
.DS_Store
Thumbs.db
*.tsbuildinfo
.dccache

# Local-only reference material (e.g. the old .NET app source) — never commit.
untracked/

# scripts/smoke.mjs's throwaway Electron profile and captured app output.
.smoke/
